name: Release

# Manually triggered from the Actions tab. Creates the tag, runs a
# final test matrix as a gate, then publishes to TestPyPI -> PyPI,
# cuts a GitHub Release, and pushes the Docker image to GHCR.
#
# workflow_dispatch is restricted to repo collaborators with write
# access by GitHub's default policy, so this is effectively owner-only.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g. 0.8.2, must match N.N.N)'
        required: true
        type: string

# Opt every job + every third-party action into the Node 24 runtime
# ahead of GitHub's 2026-06-02 hard cutover (Node 20 removed entirely
# 2026-09-16). One env beats per-action version cherry-picking and
# covers actions that haven't shipped a Node-24 release yet.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  tag:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.validate.outputs.version }}
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Validate version and create tag
      id: validate
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        VERSION="${{ inputs.version }}"
        if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+((a|b|rc)[0-9]+)?$ ]]; then
          echo "error: version must be N.N.N or N.N.N{a,b,rc}N," \
               "got '$VERSION'" >&2
          exit 1
        fi
        # A publish step can fail on a transient upload error after the
        # tag is pushed. Re-dispatching then has to be able to reuse a
        # tag that already points at this commit; a tag pointing anywhere
        # else is still a hard error.
        if git rev-parse -q --verify "refs/tags/$VERSION" >/dev/null; then
          existing=$(git rev-parse "refs/tags/$VERSION^{commit}")
          if [ "$existing" = "$(git rev-parse HEAD)" ]; then
            echo "tag '$VERSION' already at HEAD — reusing it (publish retry)"
            echo "version=$VERSION" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "error: tag '$VERSION' exists at $existing, not HEAD" >&2
          exit 1
        fi
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git tag -a "$VERSION" -m "v$VERSION"
        git push origin "$VERSION"
        echo "version=$VERSION" >> "$GITHUB_OUTPUT"

  test:
    needs: tag
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ needs.tag.outputs.version }}
        fetch-depth: 0
    - uses: ./.github/actions/setup-aiomoqt
      with:
        python-version: ${{ matrix.python-version }}
    - name: Generate self-signed cert for loopback
      run: |
        mkdir -p certs
        openssl req -x509 -newkey rsa:2048 -nodes \
          -keyout certs/key.pem -out certs/cert.pem \
          -days 1 -subj "/CN=localhost"
    - name: Run unit + integration tiers
      run: |
        python tests/release_regression_test.py \
          --test-tier unit --test-tier integration
    - name: Upload logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: regression-logs-py${{ matrix.python-version }}
        path: /tmp/aiomoqt-regression.*/

  conformance:
    needs: tag
    uses: ./.github/workflows/moq-conformance.yml
    with:
      ref: ${{ needs.tag.outputs.version }}

  publish-test:
    needs: [tag, test, conformance]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ needs.tag.outputs.version }}
        fetch-depth: 0
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine
    - name: Build and publish to TestPyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.TEST_PYPI_API_TOKEN }}
        TWINE_REPOSITORY_URL: https://test.pypi.org/legacy/
        PYTHONWARNINGS: "ignore::UserWarning"
        # Force exact version, bypassing setuptools_scm git/tree
        # detection. Insurance against dirty-tree at the tagged commit.
        SETUPTOOLS_SCM_PRETEND_VERSION_FOR_AIOMOQT: ${{ needs.tag.outputs.version }}
      run: |
        python -m build
        # TestPyPI is a staging gate; retry it and skip files a previous
        # attempt already landed. Production stays strict below.
        for attempt in 1 2 3; do
          twine upload --skip-existing --non-interactive dist/* && exit 0
          echo "TestPyPI upload attempt $attempt failed; retrying in 30s" >&2
          sleep 30
        done
        echo "error: TestPyPI upload failed after 3 attempts" >&2
        exit 1

  publish-prod:
    needs: [tag, test, publish-test]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ needs.tag.outputs.version }}
        fetch-depth: 0
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine
    - name: Build and publish to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        TWINE_REPOSITORY_URL: https://upload.pypi.org/legacy/
        PYTHONWARNINGS: "ignore::UserWarning"
        # Force exact version, bypassing setuptools_scm git/tree
        # detection. Insurance against dirty-tree at the tagged commit.
        SETUPTOOLS_SCM_PRETEND_VERSION_FOR_AIOMOQT: ${{ needs.tag.outputs.version }}
      run: |
        python -m build
        twine upload dist/*

  github-release:
    needs: [tag, publish-prod]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ needs.tag.outputs.version }}
        fetch-depth: 0
    - name: Create GitHub Release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        VERSION="${{ needs.tag.outputs.version }}"
        # PEP 440 pre-release suffix => mark the GitHub Release
        # prerelease, so it is not offered as the latest download.
        PRE=""
        [[ "$VERSION" =~ (a|b|rc)[0-9]+$ ]] && PRE="--prerelease"
        gh release create "$VERSION" \
          --title "v$VERSION" \
          --generate-notes \
          --verify-tag $PRE

  docker-publish:
    needs: [tag, publish-prod]
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ needs.tag.outputs.version }}
        fetch-depth: 0
    - name: Log in to GHCR
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Build and push Docker image
      run: |
        VERSION="${{ needs.tag.outputs.version }}"
        IMAGE=ghcr.io/${{ github.repository }}
        docker build --build-arg VERSION="$VERSION" -t "$IMAGE:$VERSION" .
        docker push "$IMAGE:$VERSION"
        # :latest tracks stable only. Interop CI and the runner pull it,
        # so a pre-release must not become everyone's default image.
        if [[ "$VERSION" =~ (a|b|rc)[0-9]+$ ]]; then
          echo "pre-release $VERSION — leaving :latest untouched"
          exit 0
        fi
        docker tag "$IMAGE:$VERSION" "$IMAGE:latest"
        docker push "$IMAGE:latest"
